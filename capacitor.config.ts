import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.kgloe.consentapp",
  appName: "Consent App",
  webDir: "out", // pages 구조인 경우-결과물
  // webDir: ".next",
};

export default config;
